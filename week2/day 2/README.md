# 🧩 Conditions · Selectors · Switch — Interactive Demo

**Date:** August 20, 2026  
**Project:** JavaScript Conditions & DOM Selectors Practice  
**Files:** `task1.html`, `style.css`, `scriptt.js`

---

## 📌 Overview

This interactive demo illustrates core JavaScript concepts:

- **Conditional logic** (`if/else`, `else if`, nested conditions, `switch`)
- **DOM selection methods** (`querySelector`, `querySelectorAll`)
- **Event handling** (click, change, mouseenter/leave)
- **Dynamic DOM updates** (changing text, styles, toggling classes)

All logic is separated into clean HTML, CSS, and JavaScript files.

---

## 🧱 File Structure
